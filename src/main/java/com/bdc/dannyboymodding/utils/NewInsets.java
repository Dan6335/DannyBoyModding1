package com.bdc.dannyboymodding.utils;

import javafx.geometry.Insets;

public class NewInsets extends Insets {
    public NewInsets(int top, int left, int bottom, int right) {
        super(top, left, bottom, right);
    }
}
